# Runtime deps for the BCI pipeline.
#
# Target env: conda env `psychopy_env` (already has numpy, scipy, matplotlib,
# pandas, mne, pylsl, pyxdf, pyserial, pyyaml, psychopy). This file installs
# the gap — packages NOT already in psychopy_env as of the first scaffold.
#
# Usage:
#   conda activate psychopy_env
#   pip install -r requirements.txt

mne-bids>=0.14
scikit-learn>=1.3
pytest>=7
